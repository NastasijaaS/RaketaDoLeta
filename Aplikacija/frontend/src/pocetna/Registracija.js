import Register from "./RegisterForma"
import Zelje from "./FormaZaZelje"

const Registracija = (props) => {

    // const { zelje } = props

    retutn(
        <div>
            {/* {zelje ? <Zelje /> : <Register />} */}
        </div>
    )
}

export default Registracija